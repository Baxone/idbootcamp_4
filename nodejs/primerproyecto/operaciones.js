const sumar = (a, b) => {
    return a + b;
}

const restar = (a, b) => {
    return a - b;
}

const multiplicar = (a, b) => {
    return a * b;
}

module.exports = {
    sum: sumar,
    rest: restar,
    mult: multiplicar
}